# 教练端 H5 应用项目结构

## 📁 目录结构

```
coach/
├── src/
│   ├── api/                      # API 接口调用
│   │   ├── coach.js              # 教练接口
│   │   ├── student.js            # 学员管理接口
│   │   ├── trainingPlan.js       # 训练方案接口
│   │   ├── course.js             # 课程接口
│   │   ├── schedule.js           # 排班接口
│   │   └── review.js             # 评价接口
│   │
│   ├── components/
│   │   └── layout/               # 布局组件
│   │       ├── NavHeader.vue     # 顶部导航
│   │       └── NavSidebar.vue    # 侧边栏菜单
│   │
│   ├── layouts/
│   │   └── CoachLayout.vue       # 教练端主布局
│   │
│   ├── router/
│   │   └── index.js              # 路由配置
│   │
│   ├── store/
│   │   └── user.js               # 用户状态管理
│   │
│   ├── utils/
│   │   └── request.js            # HTTP 请求工具
│   │
│   ├── views/                    # 页面组件
│   │   ├── auth/
│   │   │   └── Login.vue         # 登录页
│   │   ├── dashboard/
│   │   │   └── Dashboard.vue     # 工作台
│   │   ├── students/
│   │   │   ├── List.vue          # 学员列表
│   │   │   └── Detail.vue        # 学员详情
│   │   ├── training-plans/
│   │   │   ├── List.vue          # 训练方案列表
│   │   │   ├── Create.vue        # 创建方案
│   │   │   └── Templates.vue     # 方案模板
│   │   ├── courses/
│   │   │   ├── List.vue          # 课程列表
│   │   │   ├── Detail.vue        # 课程详情
│   │   │   └── Attendance.vue    # 签到管理
│   │   ├── schedule/
│   │   │   ├── Calendar.vue      # 排班日历
│   │   │   └── Request.vue       # 排班申请
│   │   ├── reviews/
│   │   │   ├── List.vue          # 评价列表
│   │   │   └── Reply.vue         # 回复评价
│   │   └── error/
│   │       └── 404.vue           # 404 页面
│   │
│   ├── App.vue                   # 根组件
│   └── main.js                   # 入口文件
│
├── public/                       # 静态资源
├── index.html                    # HTML 模板
├── package.json                  # 项目配置
├── vite.config.js               # Vite 配置
├── .env                         # 环境变量
├── .env.development             # 开发环境配置
└── .env.production              # 生产环境配置
```

## 🚀 快速开始

### 1. 安装依赖
```bash
cd coach
npm install
```

### 2. 开发环境运行
```bash
npm run dev
```
访问 http://localhost:5173

### 3. 生产构建
```bash
npm run build
```

## 📋 已完成的基础框架

- ✅ 项目初始化和依赖配置
- ✅ Vue Router 路由配置
- ✅ Pinia 状态管理
- ✅ Ant Design Vue UI 组件库集成
- ✅ HTTP 请求拦截器
- ✅ 路由守卫和认证
- ✅ 主布局组件
- ✅ 侧边栏菜单导航
- ✅ 顶部导航栏
- ✅ 工作台仪表板
- ✅ 所有页面骨架（等待功能实现）

## 🔐 身份验证

- 使用 JWT Token 进行身份验证
- Token 存储在 localStorage 中
- 自动将 Token 添加到请求头
- 401 状态自动重定向到登录页

## 🎯 路由配置

| 路由 | 组件 | 说明 |
|------|------|------|
| `/login` | Login | 登录页 |
| `/dashboard` | Dashboard | 工作台 |
| `/students` | Students/List | 学员管理 |
| `/students/:id` | Students/Detail | 学员详情 |
| `/training-plans` | TrainingPlans/List | 训练方案 |
| `/training-plans/create` | TrainingPlans/Create | 创建方案 |
| `/training-plans/templates` | TrainingPlans/Templates | 方案模板 |
| `/courses` | Courses/List | 课程管理 |
| `/courses/:id` | Courses/Detail | 课程详情 |
| `/courses/:id/attendance` | Courses/Attendance | 签到管理 |
| `/schedule` | Schedule/Calendar | 我的排班 |
| `/schedule/request` | Schedule/Request | 排班申请 |
| `/reviews` | Reviews/List | 学员评价 |
| `/reviews/:id/reply` | Reviews/Reply | 回复评价 |

## 📦 主要依赖

- `vue@3.3.4` - 前端框架
- `vue-router@4.6.3` - 路由管理
- `pinia@3.0.4` - 状态管理
- `axios@1.13.2` - HTTP 请求
- `ant-design-vue@4.2.6` - UI 组件库
- `vite@7.2.4` - 构建工具

## 📝 下一步开发

根据开发计划，接下来需要实现：

### 第一步：API 接口集成
- [ ] 封装各模块的 API 调用
- [ ] 实现登录接口集成
- [ ] 实现学员管理接口

### 第二步：核心功能实现
- [ ] 学员管理功能
- [ ] 训练方案制定
- [ ] 课程管理
- [ ] 排班管理
- [ ] 评价回复

### 第三步：数据展示
- [ ] 数据表格
- [ ] 图表统计
- [ ] 日历组件

### 第四步：联调测试
- [ ] 与后端 API 联调
- [ ] 功能测试
- [ ] 性能优化

## 🔗 相关文档

- 开发计划: `缺失功能开发计划.md`
- 后端文档: `springboot/` 目录
- 小程序文档: `uniapp/` 目录

## 💡 开发建议

1. **组件复用**：尽量使用 Ant Design Vue 的组件
2. **API 统一**：所有 API 调用都通过 `utils/request.js`
3. **状态管理**：使用 Pinia store 管理全局状态
4. **错误处理**：统一在请求拦截器中处理
5. **代码风格**：遵循 Vue 3 Composition API 风格

