# 教练端功能修复方案

## 📋 问题清单

根据教练端前后端功能完整性检查，发现以下需要修复的问题：

### 🔴 P0 - 紧急问题（影响核心功能）

#### 问题1：教练登录接口路径不匹配
- **现象**：教练端无法正常登录
- **原因**：前端调用 `/auth/coach-login`，后端实际接口为 `/user/login`
- **影响**：教练端完全无法使用
- **涉及文件**：
  - 前端：`vue3/coach/src/api/coach.js` (第5行)
  - 后端：`springboot/src/main/java/org/example/springboot/controller/UserController.java`

---

### 🟡 P1 - 高优先级问题（影响用户体验）

#### 问题2：首页数据硬编码
- **现象**：Dashboard显示的统计数据是固定值
- **原因**：前端直接写死数值，未调用API获取真实数据
- **影响**：教练看不到真实的统计信息
- **涉及文件**：
  - 前端：`vue3/coach/src/views/dashboard/Dashboard.vue` (第12-48行)

#### 问题3：后端缺少Dashboard统计接口
- **现象**：前端定义了 `getDashboardData()` API，但后端没有对应接口
- **原因**：后端 CoachController 中未实现 `/coach/dashboard` 接口
- **影响**：无法获取教练首页真实统计数据
- **涉及文件**：
  - 前端：`vue3/coach/src/api/coach.js` (第14行)
  - 后端：`springboot/src/main/java/org/example/springboot/controller/CoachController.java`

#### 问题4：教练信息获取接口缺失
- **现象**：前端定义了 `getCoachInfo()` API，但后端没有对应接口
- **原因**：后端 CoachController 中未实现 `/coach/info` 接口
- **影响**：无法查看和更新教练个人信息
- **涉及文件**：
  - 前端：`vue3/coach/src/api/coach.js` (第9行)
  - 后端：`springboot/src/main/java/org/example/springboot/controller/CoachController.java`

---

### 🟢 P2 - 中优先级问题（代码规范）

#### 问题5：API路径前缀不统一
- **现象**：训练计划API使用 `/api/training-plans`，其他使用 `/coach/xxx`
- **原因**：不同模块开发时未统一规范
- **影响**：代码维护性差，容易混淆
- **涉及文件**：
  - `vue3/coach/src/api/trainingPlan.js`

---

## 🛠️ 修复方案

### 方案A：最小化改动（推荐）
**优点**：改动少，风险低  
**缺点**：仅修复核心问题

**修复内容**：
1. 修改前端登录API路径（问题1）
2. 添加后端Dashboard接口（问题3）
3. 添加后端教练信息接口（问题4）
4. 前端Dashboard对接真实数据（问题2）

**工作量评估**：2-3小时

---

### 方案B：完整修复（建议生产环境）
**优点**：彻底解决所有问题，代码更规范  
**缺点**：改动较多，需要充分测试

**修复内容**：
1. 方案A的所有内容
2. 统一API路径规范（问题5）
3. 添加教练密码修改功能
4. 添加教练信息编辑功能

**工作量评估**：4-6小时

---

## 📝 详细修复步骤

### 步骤1：修复登录接口路径不匹配 ⭐⭐⭐⭐⭐

#### 1.1 修改前端API调用（推荐）

**文件**：`vue3/coach/src/api/coach.js`

```javascript
// 修改前
export const login = (username, password) => {
  return request.post('/auth/coach-login', { username, password })
}

// 修改后
export const login = (username, password) => {
  return request.post('/user/login', { username, password })
}
```

**或者**

#### 1.2 添加后端专用教练登录接口（可选）

**文件**：`springboot/src/main/java/org/example/springboot/controller/UserController.java`

在 UserController 中添加：

```java
@Operation(summary = "教练登录")
@PostMapping("/auth/coach-login")
public Result<UserLoginResponseDTO> coachLogin(@Valid @RequestBody UserLoginCommandDTO loginDTO) {
    log.info("教练登录请求: {}", loginDTO.getUsername());
    UserLoginResponseDTO response = userService.login(loginDTO);
    
    // 验证用户类型必须是COACH
    if (!"COACH".equals(response.getUserType())) {
        throw new BusinessException("该账号不是教练账号，请使用正确的登录入口");
    }
    
    return Result.success("登录成功", response);
}
```

**建议**：使用方案1.1（修改前端），更简单直接

---

### 步骤2：实现Dashboard统计接口 ⭐⭐⭐⭐

#### 2.1 创建DTO类

**文件**：`springboot/src/main/java/org/example/springboot/dto/response/CoachDashboardDTO.java`（新建）

```java
package org.example.springboot.dto.response;

import io.swagger.v3.oas.annotations.media.Schema;
import lombok.Data;

@Data
@Schema(description = "教练首页数据")
public class CoachDashboardDTO {
    
    @Schema(description = "学员总数")
    private Integer studentCount;
    
    @Schema(description = "训练方案总数")
    private Integer planCount;
    
    @Schema(description = "本周课程数")
    private Integer weekCourseCount;
    
    @Schema(description = "平均评分")
    private Double avgRating;
}
```

#### 2.2 在Service中添加统计方法

**文件**：`springboot/src/main/java/org/example/springboot/service/CoachService.java`

添加方法：
```java
/**
 * 获取教练首页统计数据
 */
CoachDashboardDTO getDashboardData(Long userId);
```

**文件**：`springboot/src/main/java/org/example/springboot/service/impl/CoachServiceImpl.java`

实现方法：
```java
@Override
public CoachDashboardDTO getDashboardData(Long userId) {
    // 1. 根据userId获取coachId
    GymCoach coach = this.lambdaQuery()
        .eq(GymCoach::getUserId, userId)
        .one();
    
    if (coach == null) {
        throw new BusinessException("当前用户不是教练");
    }
    
    Long coachId = coach.getId();
    
    CoachDashboardDTO dto = new CoachDashboardDTO();
    
    // 2. 统计学员数
    Integer studentCount = coachStudentMapper.selectCount(
        new LambdaQueryWrapper<GymCoachStudent>()
            .eq(GymCoachStudent::getCoachId, coachId)
    );
    dto.setStudentCount(studentCount);
    
    // 3. 统计训练方案数
    Integer planCount = trainingPlanMapper.selectCount(
        new LambdaQueryWrapper<GymTrainingPlan>()
            .eq(GymTrainingPlan::getCoachId, coachId)
            .eq(GymTrainingPlan::getStatus, 1)
    );
    dto.setPlanCount(planCount);
    
    // 4. 统计本周课程数
    LocalDate today = LocalDate.now();
    LocalDate startOfWeek = today.with(DayOfWeek.MONDAY);
    LocalDate endOfWeek = today.with(DayOfWeek.SUNDAY);
    
    Integer weekCourseCount = courseScheduleMapper.selectCount(
        new LambdaQueryWrapper<GymCourseSchedule>()
            .eq(GymCourseSchedule::getCoachId, coachId)
            .between(GymCourseSchedule::getStartTime, startOfWeek, endOfWeek)
    );
    dto.setWeekCourseCount(weekCourseCount);
    
    // 5. 获取平均评分
    dto.setAvgRating(coach.getRating() != null ? coach.getRating().doubleValue() : 0.0);
    
    return dto;
}
```

#### 2.3 在Controller中添加接口

**文件**：`springboot/src/main/java/org/example/springboot/controller/CoachController.java`

添加方法：
```java
@GetMapping("/dashboard")
@Operation(summary = "获取教练首页数据")
public Result<CoachDashboardDTO> getDashboard() {
    Long userId = JwtTokenUtils.getCurrentUserId();
    CoachDashboardDTO data = coachService.getDashboardData(userId);
    return Result.success(data);
}
```

---

### 步骤3：实现教练信息接口 ⭐⭐⭐⭐

#### 3.1 在Controller中添加接口

**文件**：`springboot/src/main/java/org/example/springboot/controller/CoachController.java`

添加方法：
```java
@GetMapping("/info")
@Operation(summary = "获取当前教练信息")
public Result<CoachResponseDTO> getCoachInfo() {
    Long userId = JwtTokenUtils.getCurrentUserId();
    
    // 根据userId查询教练
    GymCoach coach = coachService.lambdaQuery()
        .eq(GymCoach::getUserId, userId)
        .one();
    
    if (coach == null) {
        return Result.error("当前用户不是教练");
    }
    
    CoachResponseDTO response = coachService.getCoachById(coach.getId());
    return Result.success(response);
}

@PutMapping("/info")
@Operation(summary = "更新教练信息")
public Result<CoachResponseDTO> updateCoachInfo(@Valid @RequestBody CoachUpdateDTO updateDTO) {
    Long userId = JwtTokenUtils.getCurrentUserId();
    
    // 根据userId查询教练
    GymCoach coach = coachService.lambdaQuery()
        .eq(GymCoach::getUserId, userId)
        .one();
    
    if (coach == null) {
        return Result.error("当前用户不是教练");
    }
    
    CoachResponseDTO response = coachService.updateCoach(coach.getId(), updateDTO);
    return Result.success(response);
}
```

---

### 步骤4：前端Dashboard对接真实数据 ⭐⭐⭐

**文件**：`vue3/coach/src/views/dashboard/Dashboard.vue`

修改 script 部分：

```vue
<script setup>
import { ref, onMounted } from 'vue'
import { 
  TeamOutlined, 
  FileTextOutlined, 
  CalendarOutlined, 
  StarOutlined,
  ClockCircleOutlined,
  FormOutlined,
  BarChartOutlined,
  CheckCircleOutlined
} from '@ant-design/icons-vue'
import { getDashboardData } from '@/api/coach'
import { message } from 'ant-design-vue'

// 统计数据
const stats = ref({
  studentCount: 0,
  planCount: 0,
  weekCourseCount: 0,
  avgRating: 0
})

// 加载中状态
const loading = ref(true)

// 加载统计数据
const loadDashboardData = async () => {
  try {
    loading.value = true
    const response = await getDashboardData()
    stats.value = response.data
  } catch (error) {
    console.error('加载首页数据失败:', error)
    message.error('加载统计数据失败，请刷新重试')
  } finally {
    loading.value = false
  }
}

onMounted(() => {
  loadDashboardData()
})
</script>
```

修改 template 部分的数据绑定：

```vue
<!-- 将硬编码的数值改为动态绑定 -->
<a-statistic title="我的学员" :value="stats.studentCount" suffix="人" />
<a-statistic title="训练方案" :value="stats.planCount" suffix="个" />
<a-statistic title="本周课程" :value="stats.weekCourseCount" suffix="节" />
<a-statistic title="平均评分" :value="stats.avgRating" suffix="分" :precision="1" />
```

---

### 步骤5：统一API路径规范（可选） ⭐⭐

**文件**：`vue3/coach/src/api/trainingPlan.js`

将所有 `/api/training-plans` 改为 `/training-plans`：

```javascript
// 修改前
export const getTrainingPlans = (params) => {
  return request.get('/api/training-plans/page', { params })
}

// 修改后
export const getTrainingPlans = (params) => {
  return request.get('/training-plans/page', { params })
}
```

---

## ✅ 验证清单

完成修复后，请按以下清单验证功能：

### 登录功能
- [ ] 教练可以正常登录
- [ ] 登录失败有正确的错误提示
- [ ] 登录成功后跳转到Dashboard
- [ ] Token正确保存到localStorage

### Dashboard首页
- [ ] 学员数统计正确
- [ ] 训练方案数统计正确
- [ ] 本周课程数统计正确
- [ ] 平均评分显示正确
- [ ] 数据加载失败有错误提示

### 教练信息
- [ ] 可以查看教练个人信息
- [ ] 可以更新教练信息
- [ ] 更新成功有提示

### 其他功能
- [ ] 学员管理功能正常
- [ ] 排班管理功能正常
- [ ] 评价管理功能正常
- [ ] 课程管理功能正常
- [ ] 训练计划功能正常

---

## 📊 修复优先级建议

### 第一阶段（必须完成）
1. ✅ 修复登录接口（步骤1）- 30分钟
2. ✅ 实现Dashboard统计接口（步骤2）- 1小时
3. ✅ 实现教练信息接口（步骤3）- 30分钟

**预计总时间**：2小时

### 第二阶段（建议完成）
4. ✅ Dashboard对接真实数据（步骤4）- 30分钟
5. ✅ 统一API路径（步骤5）- 30分钟

**预计总时间**：1小时

---

## 🚀 快速修复脚本

如果选择方案A（最小化改动），可以按以下顺序执行：

```bash
# 1. 修改前端登录API
# 编辑 vue3/coach/src/api/coach.js

# 2. 创建DTO
# 创建 springboot/src/main/java/org/example/springboot/dto/response/CoachDashboardDTO.java

# 3. 修改Service和Controller
# 编辑相关Java文件

# 4. 重启后端服务
cd springboot
mvn spring-boot:run

# 5. 修改前端Dashboard
# 编辑 vue3/coach/src/views/dashboard/Dashboard.vue

# 6. 重启前端服务
cd vue3/coach
npm run dev
```

---

## ⚠️ 注意事项

1. **备份代码**：修改前先备份或提交当前代码
2. **测试数据**：确保数据库有测试数据（教练、学员、课程等）
3. **依赖注入**：Service实现中需要注入相关Mapper
4. **异常处理**：添加适当的异常处理和日志
5. **权限验证**：确保所有接口都验证了用户身份

---

## 📞 遇到问题？

如果在修复过程中遇到问题：

1. **编译错误**：检查import语句和类名是否正确
2. **404错误**：检查API路径是否匹配
3. **401错误**：检查Token是否正确传递
4. **数据为空**：检查数据库是否有测试数据
5. **其他错误**：查看控制台日志，定位具体错误位置

---

**最后更新**：2025-12-19  
**文档版本**：v1.0
